# Static export config — works with Netlify Drop OR Git.
# `next build` with output:"export" in next.config.js produces an `out/` folder.
# No Next.js runtime plugin is needed for a static export.

[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
